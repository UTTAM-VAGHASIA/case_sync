import 'package:flutter/material.dart';

class InternListScreen extends StatelessWidget {
  const InternListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Intern List'),
      ),
      body: Center(
        child: Text('Intern List Screen'),
      ),
    );
  }
}
