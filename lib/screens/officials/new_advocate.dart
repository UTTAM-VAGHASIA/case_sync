import 'package:flutter/material.dart';

class NewAdvocateScreen extends StatelessWidget {
  const NewAdvocateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('New Advocate'),
      ),
      body: Center(
        child: Text('New Advocate Screen'),
      ),
    );
  }
}
